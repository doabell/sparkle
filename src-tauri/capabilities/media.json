{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "media",
    "description": "Capability to access native OS media controls",
    "windows": ["main"],
    "permissions": ["media:default"]
}
